name: Run unit tests
run-name: Unit tests runner
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo 'Running tests'
          ./runtests unit
          echo 'Unit tests completed'
